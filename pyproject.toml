[project]
name = "gate-runner"
version = "0.1.0"
description = "Prime Lab workspace for the Gate Runner honest finance-RL environment"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "verifiers>=0.1.14",
]
